# Python依赖包
# 项目: Docker Build Evaluation System v2.0

# === 核心依赖 ===
pyyaml>=6.0                 # YAML配置文件解析
requests>=2.28.0            # HTTP请求
python-dotenv>=0.19.0       # 环境变量加载
tenacity>=8.0.0             # API重试机制

# === 测试依赖 ===
pytest>=7.0.0               # 单元测试框架
pytest-cov>=4.0.0           # 测试覆盖率

# === 开发依赖（可选）===
# black>=23.0.0             # 代码格式化
# flake8>=6.0.0             # 代码检查
# mypy>=1.0.0               # 类型检查
# pre-commit>=3.0.0         # Git钩子
